# .github/workflows/sync-to-production.yaml
name: Sync main to sandbox-production

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder job
        run: echo "Workflow removed; no action is taken."
