# JARVIS 5.0 - Dependências Completas
# Instalação: pip install -r requirements_advanced.txt

# === CORE ===
numpy>=1.24.0
opencv-python>=4.8.0
pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# === AI & ML ===
# Processamento Local
torch>=2.6.0  # SECURITY: Minimum 2.6.0 required to avoid CVEs (RCE, heap overflow, use-after-free)
transformers>=4.30.0
sentence-transformers>=2.2.0

# Visão Computacional
easyocr>=1.7.0  # OCR multilíngue avançado
pytesseract>=0.3.10  # OCR básico
ultralytics>=8.0.0  # YOLO v8

# Processamento de Voz
openai-whisper>=20231117  # STT neural
pyttsx3>=2.90  # TTS (fallback)
vosk>=0.3.45  # Wake word offline
TTS>=0.22.0  # XTTS-v2 neural TTS
# pyannote.audio>=3.0.0  # Diarização (opcional)
# librosa>=0.10.0  # Análise de áudio (opcional)

# === CLOUD AI ===
google-generativeai>=0.3.0  # Gemini
anthropic>=0.7.0  # Claude (opcional)
openai>=1.0.0  # OpenAI (opcional)

# === AUTOMAÇÃO ===
pyautogui>=0.9.54
pywinauto>=0.6.8; sys_platform == 'win32'
psutil>=5.9.0
pywin32>=306; sys_platform == 'win32'  # Productivity tracking

# === GESTURE RECOGNITION ===
mediapipe>=0.10.0  # Hand tracking

# === SEGURANÇA ===
cryptography>=41.0.0  # AES-256, Fernet
bcrypt>=4.0.0  # Hashing de senhas

# === DATABASE ===
sqlalchemy>=2.0.0
sqlite3  # Built-in

# === GUI ===
customtkinter>=5.2.0
tkinter  # Built-in

# === FACE RECOGNITION ===
# Requer CMake instalado (auto-instalado pelo maintenance_manager)
face-recognition>=1.3.0
dlib>=19.24.0

# === UTILITIES ===
colorama>=0.4.6
tqdm>=4.66.0
pyyaml>=6.0

# === DEVELOPMENT ===
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0

# === OPTIONAL (Performance) ===
# onnxruntime-gpu>=1.16.0  # GPU acceleration
# tensorrt>=8.6.0  # NVIDIA optimization
