# ============================================
# JARVIS SINGULARITY - Dependências Unificadas
# ============================================

# === CORE ===
asyncio-compat>=0.1.0
pyyaml>=6.0
python-dotenv>=1.0.0
colorama>=0.4.6

# === PROCESSAMENTO DE DADOS ===
numpy>=1.24.0
pandas>=2.0.0

# === VISÃO COMPUTACIONAL ===
opencv-python>=4.8.0
pillow>=10.0.0
easyocr>=1.7.0
pytesseract>=0.3.10
ultralytics>=8.0.0  # YOLO v8

# === UI AUTOMATION (Windows) ===
uiautomation>=2.0.0; sys_platform == 'win32'
pywinauto>=0.6.8; sys_platform == 'win32'
pyautogui>=0.9.54
pywin32>=306; sys_platform == 'win32'

# === HIVE MIND (Consciência Distribuída) ===
chromadb>=0.4.0
sentence-transformers>=2.2.0
# rclone - Binário externo (instalar separadamente)

# === CÉREBRO HÍBRIDO ===
# Groq API (Llama 3 70B - Rápido)
groq>=0.4.0

# Google Gemini (Profundo + Vision)
google-generativeai>=0.3.0

# Anthropic Claude (Opcional)
anthropic>=0.7.0

# OpenAI (Opcional)
openai>=1.0.0

# === PROCESSAMENTO DE VOZ ===
# Speech-to-Text
openai-whisper>=20231117
faster-whisper>=0.10.0
vosk>=0.3.45

# Text-to-Speech
pyttsx3>=2.90
edge-tts>=6.1.0
TTS>=0.22.0  # XTTS-v2

# Voice Activity Detection
torch>=2.0.0
torchaudio>=2.0.0
# silero-vad via torch.hub

# === RECONHECIMENTO ===
# Face Recognition
face-recognition>=1.3.0
dlib>=19.24.0

# Gesture Recognition
mediapipe>=0.10.0

# === IoT REVERSO ===
fauxmo>=0.7.0  # Fake Alexa device
tinytuya>=1.13.0  # Controle Tuya
python-kasa>=0.5.0  # Controle TP-Link Kasa

# === INTERFACE HUD ===
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0

# === AUTOMAÇÃO ===
psutil>=5.9.0
watchdog>=3.0.0  # File monitoring
schedule>=1.2.0  # Task scheduling

# === SEGURANÇA ===
cryptography>=41.0.0
bcrypt>=4.0.0

# === DATABASE ===
sqlalchemy>=2.0.0
# sqlite3 - Built-in

# === NETWORKING ===
requests>=2.31.0
aiohttp>=3.9.0
websockets>=12.0

# === UTILITIES ===
tqdm>=4.66.0
click>=8.1.0

# === DEVELOPMENT ===
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# === OPTIONAL (Performance) ===
# onnxruntime-gpu>=1.16.0  # GPU acceleration
# tensorrt>=8.6.0  # NVIDIA optimization

# ============================================
# NOTAS DE INSTALAÇÃO
# ============================================
# 
# 1. Rclone (Sync com Google Drive):
#    Windows: choco install rclone
#    Ou baixe: https://rclone.org/downloads/
#
# 2. CMake (para dlib/face_recognition):
#    Windows: choco install cmake
#    Ou: pip install cmake
#
# 3. Modelos de IA:
#    - Vosk PT-BR: Auto-download via maintenance_manager
#    - Whisper: Auto-download no primeiro uso
#    - YOLO v8: Auto-download via ultralytics
#
# 4. APIs Gratuitas Necessárias:
#    - Groq: https://console.groq.com
#    - Google AI Studio: https://makersuite.google.com
#
# ============================================
