[pytest]
# Rodar somente os testes dentro de tests/
testpaths = tests
python_files = test_*.py
<<<<<<< HEAD
norecursedirs = scripts tools auto_generated docs .venv venv build dist tests/legacy tests/suites tests/debug
# Evita coleta de utilitários e scripts de debug
filterwarnings = ignore::DeprecationWarning
addopts = -ra --ignore=tests/unit/test_memory.py --ignore=tests/unit/test_god_mode.py --ignore=tests/unit/test_chromadb_reset.py

# Marcas personalizadas usadas pelo projeto
markers =
    integration: marca testes de integração/ambientais (pesados)
    native: marca testes que exigem dependências nativas (torch/onnxruntime/chromadb)
    slow: marca testes lentos (execução opcional)
=======
norecursedirs = scripts tools auto_generated docs .venv venv build dist
# Evita coleta de utilitários e scripts de debug
filterwarnings = ignore::DeprecationWarning
>>>>>>> dev-new-version
