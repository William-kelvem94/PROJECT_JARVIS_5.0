# ============================================================================
# JARVIS CHAMELEON - HYBRID Profile Requirements (Light GPU Support)
# ============================================================================
# For: GTX 1050 Ti, GTX 1650, 4-6GB VRAM
# Features: All + GPU acceleration + Light training (4-bit)
# Training: Limited (inference + light fine-tuning)
# ============================================================================

# CRITICAL: numpy < 2.0 for compatibility
numpy==1.26.4

# -------------------------------------------------------------------------
# EVERYTHING FROM LITE PROFILE
# -------------------------------------------------------------------------

# Interface
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
pyqtgraph==0.13.3

# System
psutil==5.9.8
pywin32==306; sys_platform == 'win32'
pyautogui==0.9.54
keyboard==0.13.5
mouse==0.7.1

# Vision
opencv-python==4.9.0.80
Pillow==10.3.0
mss==9.0.1
face-recognition==1.3.0
dlib==19.24.2

# Audio
pyaudio==0.2.14
sounddevice==0.4.6
soundfile==0.12.1
librosa==0.10.1
pyttsx3==2.90
gtts==2.5.0

# Utilities
requests==2.31.0
aiohttp==3.13.3
python-dotenv==1.0.1
PyYAML==6.0.1
jsonlines==4.0.0
pandas==2.1.4
h5py==3.10.0

# Database
chromadb==0.4.22

# Logging
colorama==0.4.6
tqdm==4.66.1

# -------------------------------------------------------------------------
# HYBRID-SPECIFIC: PyTorch + Quantization
# -------------------------------------------------------------------------

# PyTorch (CUDA/CPU auto-detect)
torch==2.6.0
torchaudio==2.6.0
torchvision==0.21.0

# 4-bit quantization for memory efficiency
bitsandbytes==0.42.0

# Transformers for model loading
transformers==4.37.2
accelerate==0.26.1
sentencepiece==0.1.99

# YOLO for object detection (GPU)
ultralytics==8.1.0

# Advanced audio (with torch)
edge-tts==6.1.9

# -------------------------------------------------------------------------
# LIGHT TRAINING SUPPORT
# -------------------------------------------------------------------------
datasets==2.16.1
evaluate==0.4.1
scikit-learn==1.4.0

# Total: 58 packages (with GPU support + light training)