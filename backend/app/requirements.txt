# Default CPU install used by CI and simple pip workflows.
# The Windows launcher installs a hardware-specific torch profile first.

-r requirements-torch-cpu.txt
-r requirements-base.txt

# resemblyzer is installed by scripts/setup-venv.bat with --no-deps after
# webrtcvad-wheels is present, avoiding native webrtcvad compilation on Windows.
