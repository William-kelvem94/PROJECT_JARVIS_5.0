# ─────────────────────────────────────────────────────────────────────────────
# JARVIS 5.0 - Backend Requirements
#
# IMPORTANTE: 'resemblyzer' foi removido daqui intencionalmente.
# Ele e instalado com --no-deps no start-jarvis.bat para evitar que o pip
# tente compilar 'webrtcvad' do zero (requer C++ Build Tools no Windows).
# O 'webrtcvad-wheels' e pre-instalado antes e satisfaz o import corretamente.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core API
fastapi
uvicorn[standard]
pydantic-settings
python-dotenv
requests
loguru
aiohttp
psutil
torch
torchvision --index-url https://download.pytorch.org/whl/cpu

# ── Browser Automation
playwright

# ── Screen / GPU monitoring
GPUtil
mss
Pillow

# ── Perception: Face
mediapipe
opencv-python
numpy

# ── Perception: Voice - Wake Word
openwakeword

# ── Perception: Voice - Speaker ID
# resemblyzer e instalado via --no-deps no launcher (ver start-jarvis.bat)
# webrtcvad-wheels e instalado antes como substituto ao webrtcvad nativo
webrtcvad-wheels

# ── Perception: Voice - Transcricao Offline
faster-whisper

# ── Perception: Audio Input
sounddevice

# ── Perception: Text-to-Speech Local
edge-tts