# ─────────────────────────────────────────────────────────────────────────────
# JARVIS 5.0 - Backend Requirements
# NOTA: webrtcvad-wheels DEVE vir antes de resemblyzer para evitar compilacao C++
# ─────────────────────────────────────────────────────────────────────────────

# ── Core API
fastapi
uvicorn[standard]
pydantic-settings
python-dotenv
requests
loguru
aiohttp
psutil

# ── LiveKit AI Stack
livekit-agents
livekit-plugins-openai
livekit-plugins-google
livekit-plugins-deepgram
livekit-plugins-silero
livekit-plugins-noise-cancellation

# ── Browser Automation
playwright

# ── Screen / GPU monitoring
GPUtil
mss
Pillow

# ── Perception: Face
mediapipe
opencv-python
numpy

# ── Perception: Voice - Wake Word
openwakeword

# ── Perception: Voice - Speaker ID
# webrtcvad-wheels DEVE ser instalado antes do resemblyzer (substitui webrtcvad sem compilador C++)
webrtcvad-wheels
resemblyzer

# ── Perception: Voice - Transcricao Offline
faster-whisper

# ── Perception: Audio Input
sounddevice