# ============================================================================
# JARVIS SINGULARITY - Dependências Travadas (Versões Estáveis)
# ============================================================================
# ÚLTIMA ATUALIZAÇÃO: 2026-02-05
# OBJETIVO: Resolver conflitos numpy 2.x vs torch/whisper
# ============================================================================

# -------------------------------------------------------------------------
# CORE CRÍTICO - VERSÕES TRAVADAS (NÃO ALTERAR SEM TESTES)
# -------------------------------------------------------------------------

# NumPy < 2.0 - OBRIGATÓRIO para compatibilidade com Torch/Whisper
numpy==1.26.4

# PyTorch (versão estável CPU Windows para evitar DLL Error 1114)
torch==2.2.2+cpu; sys_platform == 'win32'
torchvision==0.17.2+cpu; sys_platform == 'win32'
torchaudio==2.2.2+cpu; sys_platform == 'win32'
--find-links https://download.pytorch.org/whl/cpu/torch_stable.html

# Cryptography (versão estável que resolve erro PBKDF2)
cryptography==42.0.5

# -------------------------------------------------------------------------
# INTERFACE GRÁFICA - PyQt6 (Dual Mode: HUD + Dashboard)
# -------------------------------------------------------------------------
PyQt6==6.6.1
PyQt6-Qt6==6.6.1
PyQt6-sip==13.6.0
pyqtgraph==0.13.3  # Advanced plotting for Control Dashboard

# -------------------------------------------------------------------------
# VISÃO COMPUTACIONAL (Omni-Vision System)
# -------------------------------------------------------------------------
opencv-python==4.9.0.80
Pillow==10.3.0  # Atualizado de 10.2.0 para corrigir buffer overflow (CVE)
transformers==4.30.2
ultralytics==8.1.0  # YOLOv8 para detecção de objetos/UI

# Captura de tela ultra-rápida (multi-monitor)
mss==9.0.1

# Reconhecimento facial (FaceID biométrico)
# NOTA: face-recognition e dlib podem falhar no Windows sem Visual C++ build tools
# Se a instalação falhar, pule estas dependências - o sistema funcionará sem FaceID
# Para instalar depois: pip install dlib face-recognition
face-recognition==1.3.0
# dlib==19.24.2  # OPCIONAL - Comentado por padrão (requer compilação C++)

# MediaPipe para detecção de gestos
mediapipe==0.10.9

# -------------------------------------------------------------------------
# PROCESSAMENTO DE VOZ AVANÇADO (Local + Biometria)
# -------------------------------------------------------------------------
# Whisper (OpenAI) - Requer numpy < 2.0
openai-whisper==20231117

# Faster-Whisper para STT ultra-rápido (GPU/CPU)
faster-whisper==1.0.3

# TTS/STT
pyttsx3==2.90
SpeechRecognition==3.10.4

# Vosk (alternativa offline)
vosk==0.3.45

# Edge TTS para síntese de voz de alta qualidade
edge-tts==6.1.9

# Audio processing
librosa==0.10.1
soundfile==0.12.1
pyaudio==0.2.14
pygame==2.6.1  # For response caching audio playback (P1.3)

# Speaker verification
resemblyzer==0.1.1.dev0

# -------------------------------------------------------------------------
# AUTOMAÇÃO DE UI & GOD MODE (Deep Windows Control)
# -------------------------------------------------------------------------
pyautogui==0.9.54
pyperclip==1.8.2

# Windows API (God Mode)
pywin32==306; sys_platform == 'win32'
pycaw==20231129; sys_platform == 'win32'  # Per-app audio control
comtypes==1.2.0; sys_platform == 'win32'  # COM interface
WMI==1.5.1; sys_platform == 'win32'  # Windows Management Instrumentation

# System Control Extensions (Device Manager Advanced)
screen-brightness-control==0.22.3; sys_platform == 'win32'  # Monitor brightness control
watchdog==4.0.0  # File system monitoring

# -------------------------------------------------------------------------
# LLM & AI
# -------------------------------------------------------------------------
# Google Gemini
google-generativeai==0.3.2

# OpenAI (opcional)
openai==1.12.0

# Anthropic Claude (opcional)
anthropic==0.8.1

# Groq (rápido e gratuito)
groq==0.4.2

# -------------------------------------------------------------------------
# VECTOR DATABASE & EMBEDDINGS (RAG)
# -------------------------------------------------------------------------
chromadb==0.4.22
# sentence-transformers with FULL extras for CrossEncoder (P1.4 RAG Reranking)
sentence-transformers[all]>=2.3.0

# -------------------------------------------------------------------------
# WEB & HTTP (Web Fallback quando conhecimento local falhar)
# -------------------------------------------------------------------------
requests==2.31.0
aiohttp==3.13.3  # Atualizado de 3.9.3 para corrigir TODAS vulnerabilidades (DoS e zip bomb CVEs)
beautifulsoup4==4.12.3
lxml==5.1.0
googlesearch-python==1.2.3  # Google Search API para busca web

# FastAPI (para servidor web - opcional)
fastapi==0.109.2
uvicorn==0.27.1

# -------------------------------------------------------------------------
# DATABASE
# -------------------------------------------------------------------------
sqlalchemy==2.0.25

# -------------------------------------------------------------------------
# UTILITIES
# -------------------------------------------------------------------------
python-dotenv==1.0.1
psutil==5.9.8
aiofiles==25.1.0
colorlog==6.8.2
tqdm==4.66.2

# PyYAML para validação de config.yaml (CRÍTICO para validator)
PyYAML==6.0.1

# ============== P0/P1 AUTO-INSTALL UPGRADES ==============
# NOTA: Estas bibliotecas são instaladas automaticamente na inicialização
#       do JARVIS para habilitar features world-class

# P0.6 Wake Word Detection (Privacy + 50% menos CPU)
pvporcupine==3.0.0            # Porcupine - requer API key gratuita
# openwakeword==0.5.1         # Alternativa (comentado, use porcupine)

# P0.7 Voice Cloning XTTS-v2 (Personalização de voz)
# AVISO: TTS requer Visual C++ Build Tools no Windows
#        Se instalação falhar, o sistema funcionará com Edge-TTS/pyttsx3
TTS==0.22.0                   # Coqui TTS - opcional mas recomendado

# P1.2 Noise Reduction (Audio Enhancement)
noisereduce==3.0.2            # Redução de ruído - +20% STT accuracy

# -------------------------------------------------------------------------
# DEVELOPMENT & TESTING
# -------------------------------------------------------------------------
pytest==7.4.4
pytest-asyncio==0.21.1

# -------------------------------------------------------------------------
# NOTAS IMPORTANTES
# -------------------------------------------------------------------------
# 1. NumPy 2.x QUEBRA torch e whisper - NUNCA atualize para 2.x
# 2. Torch 2.6.0+ corrige erro register_pytree_node E vulnerabilidades de segurança
# 3. Se pip tentar instalar numpy 2.x, force reinstall:
#    pip install --force-reinstall numpy==1.26.4
# 4. Cryptography 42.0.5 resolve erro de importação PBKDF2
# 5. PyQt6 6.6.1 é a última versão estável testada
# 6. Total de 42 dependências incluindo mss, edge-tts, mediapipe, face-recognition
# 7. Para instalar tudo: pip install -r requirements_singularity.txt
# -------------------------------------------------------------------------

# ============================================================================
# TROUBLESHOOTING
# ============================================================================
# 
# ERRO: "numpy 2.0 is not compatible with this version of torch"
# SOLUÇÃO: pip uninstall numpy && pip install numpy==1.26.4
#
# ERRO: "module 'torch.utils._pytree' has no attribute 'register_pytree_node'"
# SOLUÇÃO: pip install --upgrade torch==2.6.0 torchaudio==2.6.0
#
# ERRO (SEGURANÇA): "PyTorch torch.load RCE vulnerability"
# SOLUÇÃO: pip install --upgrade torch>=2.4.0
#
# ERRO (SEGURANÇA): "Pillow buffer overflow"
# SOLUÇÃO: pip install --upgrade Pillow==10.3.0
#
# ERRO (SEGURANÇA): "aiohttp DoS vulnerability"
# SOLUÇÃO: pip install --upgrade aiohttp==3.13.3
#
# ERRO: "cannot import name 'PBKDF2' from 'cryptography'"
# SOLUÇÃO: pip install --upgrade cryptography==42.0.5
#
# ERRO: "No module named 'PyQt6'"
# SOLUÇÃO: pip install PyQt6==6.6.1
#
# ERRO: "No module named 'mss'" (screen capture)
# SOLUÇÃO: pip install mss==9.0.1
#
# ERRO: "No module named 'edge_tts'" (voice controller)
# SOLUÇÃO: pip install edge-tts==6.1.9
#
# ERRO: "No module named 'mediapipe'" (gesture controller)
# SOLUÇÃO: pip install mediapipe==0.10.9
#
# ERRO: "No module named 'face_recognition'" (camera controller)
# SOLUÇÃO: pip install face-recognition==1.3.0
#
# ERRO: "torch not found"
# SOLUÇÃO: pip install torch>=2.4.0 --index-url https://download.pytorch.org/whl/cpu
# ============================================================================

# ============================================================================
# 🚀 P0 UPGRADES - JARVIS 5.0 WORLD-CLASS AI
# ============================================================================

# Wake Word Detection (Privacy + Performance)
pvporcupine==3.0.0        # Porcupine wake word engine
# openwakeword==0.5.1      # Alternative (lighter)

# Voice Cloning (Personalização)
TTS==0.22.0               # Coqui TTS com XTTS-v2

# Audio Enhancement
noisereduce==3.0.2        # Noise reduction preprocessing

# ============================================================================
# P1 OPTIMIZATIONS - Performance Enhancements
# ============================================================================
# The following P1 features are already included in existing dependencies:
# - Semantic Caching: Built-in (vision_system.py)
# - Response Caching: Built-in (voice_controller.py)
# - RAG Reranking: Included in sentence-transformers (CrossEncoder)
# - Jina Embeddings v3: Available via sentence-transformers
# - UI-Specific YOLO: Built-in (vision_learner.py)
#
# To enable full P1 capabilities, ensure sentence-transformers is installed with:
# pip install sentence-transformers[all]

# RAG Upgrade (Better embeddings)
# sentence-transformers>=2.3.0  # Já existe

# ============================================================================
# 🔮 NEURAL EXPANSION PHASE 1 - Advanced Senses (Sprint 1-5)
# ============================================================================
librosa>=0.10.0               # Advanced audio analysis (emotion, tone)
pyannote.audio                # Speaker diarization
onnxruntime==1.17.0           # Required for ONNX model inference acceleration (CPU)
# onnxruntime-gpu             # GPU version (uncomment if CUDA available)
silero-vad==6.2.0             # Neural Voice Activity Detection (Sprint 1)
networkx>=2.8                 # Knowledge Graph foundation (Sprint 2)
matplotlib>=3.7.0             # Visualization for graphs
pyqtgraph>=0.13.3             # Advanced plotting for Neural Dashboard (Sprint 5)
